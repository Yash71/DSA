#include<iostream>
#include<queue>
using namespace std;

int main(){
  queue<int>q;
  q.empty(); //checks if the queue is empty
  q.size(); //returns the size of the queue
  q.front(); //returns a reference to the first element
  q.back(); //returns a reference to the last element
  q.push(x); //adds the element to the top of the queue
  q.pop(); //deletes the topmost element from the queue
  
}